# EVT-011 — reverse Game/Chat sweep

Date: 2026-09-16
Transport: hidraw, opened read-only
Action: slow sweep from Game extreme to Chat extreme

## Observed Report `0x10` payloads

```text
0f, 0e, 0d, 0c, 0b, 0a, 08, 06, 05, 04, 03, 02, 01, 00
```

Combined with EVT-010, this confirms 15 logical positions:

- Chat side: `0x00–0x06`, normalized `-7` through `-1`
- Center detent: `0x08`, normalized `0`
- Game side: `0x0a–0x10`, normalized `+1` through `+7`

Values `0x07` and `0x09` are deliberately absent on both sweep directions.
They form a two-code gap around the physical center detent rather than missing
monitor events.
