# EVT-010 — slow Game/Chat sweep

Date: 2026-09-16
Transport: hidraw, opened read-only
Action: slow sweep from the Chat side toward the Game extreme

## Observed payloads for Report `0x10`

```text
02, 03, 04, 05, 06, 08, 0a, 0b, 0c, 0d, 0e, 0f, 10
```

## Conclusions

- The earlier hypothesis that the dial emits only even values is disproved.
- Confirmed numeric range remains `0x00` (Chat) through `0x10` (Game).
- Unit steps occur across most of the physical travel.
- Values `0x07` and `0x09` were absent immediately around the center `0x08`.
  This may indicate a center detent/deadband or skipped events; a reverse sweep
  is required.
- Values `0x00` and `0x01` were not emitted in this capture because monitoring
  began with the dial already near/on the Chat extreme; `0x00` was confirmed in
  EVT-009.
