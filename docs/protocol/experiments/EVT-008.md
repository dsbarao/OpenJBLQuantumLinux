# EVT-008 — main volume wheel

Date: 2026-09-16
Transport observed: hidraw, read-only
Action: main volume wheel moved in both increase and decrease directions

## Result

No HID Input Report appeared in the monitor for either direction.

ALSA mixer state was saved before and after moving the wheel. The files were
byte-for-byte identical (`cmp` exit 0) with the same SHA-256:

```text
afcdc2b576bcc94c4af2ef19b413c741ab634222d48330149b329afa7f4f0b44
```

The observed controls remained:

- Game/PCM index 0: 51/74, 69%, -23 dB
- Chat/PCM index 1: 64/74, 86%, -10 dB
- Microphone: 74/74, 100%, 0 dB

## Interpretation

The main volume wheel is confirmed to be local within the headset/dongle for
the passively observable Linux interfaces: it produces neither a hidraw Input
Report nor a change in the ALSA mixer controls.

This does not prove that volume has no representation in Feature Reports or
vendor commands. No active query was attempted.
