[package]
# Compatibility: keep the installed package and CLI name; see docs/rebranding.md.
name = "openjblquantum"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "JanBaLinux SonicCore — open-source gaming headset control and audio platform for Linux"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zbus = "5"
